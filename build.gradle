import com.omottec.demoapp.BuildSrcDeps

// Top-level build file where you can add configuration options common to all sub-projects/modules.

buildscript {
    repositories {
        jcenter()
        mavenLocal()
        mavenCentral()
        google()
    }
    dependencies {
        classpath BuildSrcDeps.AGP_CLASSPATH
        classpath BuildSrcDeps.GREENDAO_CLASSPATH
        classpath BuildSrcDeps.ASPECTJ_CLASSPATH
        classpath BuildSrcDeps.KOTLIN_CLASSPATH
        classpath BuildSrcDeps.OMOTTEC_ASM_CLASSPATH
        classpath BuildSrcDeps.OMOTTEC_JAVASSIST_CLASSPATH

        // NOTE: Do not place your application dependencies here; they belong
        // in the individual module build.gradle files
    }
}

plugins {
    id 'com.omottec.demoapp.plugin'
}

allprojects {
    repositories {
        jcenter()
        mavenCentral()
        google()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}

apply from: rootProject.file('gradles/build_monitor.gradle')